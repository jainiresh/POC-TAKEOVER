"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _exceptions = require("../../exceptions");

var _LocationFileLine = _interopRequireDefault(require("./LocationFileLine"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const ASSERT_TRACKING_EXECUTE_LINE = 3;

class LocationAssertTracking extends _LocationFileLine.default {
  constructor(configuration, factory) {
    configuration.filename = "AssertTrackingServiceDummy.js";
    configuration.lineno = ASSERT_TRACKING_EXECUTE_LINE;
    super(configuration, factory);
    this.includeExternals = true;
  }

  addAug(triggerServices, aug) {
    super.addAug(triggerServices, aug);
    let assertTrackingService = triggerServices.getService("AssertTrackingService");

    if (assertTrackingService === undefined) {
      throw new _exceptions.RookAssertTrackingServiceNotFound();
    }

    assertTrackingService.addAug(aug);
  }

}

exports.default = LocationAssertTracking;