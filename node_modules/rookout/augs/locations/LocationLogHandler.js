"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _exceptions = require("../../exceptions");

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

const config = require('../../config');

class LocationLogHandler {
  constructor(configuration, factory) {
    _defineProperty(this, "minTime", config.RateLimitConfiguration.NON_INSPECTOR_RATE_LIMITER_MINIMUM_DURATION_TIME_NS);
  }

  addAug(triggerServices, aug) {
    let loggingService = triggerServices.getService("loggingService");

    if (loggingService === undefined) {
      throw new _exceptions.RookLoggingServiceNotFound();
    }

    loggingService.addLoggingAug(aug);
  }

}

exports.default = LocationLogHandler;