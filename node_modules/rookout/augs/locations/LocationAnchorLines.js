"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _exceptions = require("../../exceptions");

var _LocationFileLine = _interopRequireDefault(require("./LocationFileLine"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const ANCHOR_LINES_EXECUTE_LINE = 3;

class LocationAnchorLines extends _LocationFileLine.default {
  constructor(configuration, factory) {
    configuration.filename = "AnchorLinesServiceDummy.js";
    configuration.lineno = ANCHOR_LINES_EXECUTE_LINE;
    super(configuration, factory);
    this.includeExternals = true;
  }

  addAug(triggerServices, aug) {
    super.addAug(triggerServices, aug);
    let anchorLinesService = triggerServices.getService("AnchorLinesService");

    if (anchorLinesService === undefined) {
      throw new _exceptions.RookAnchorLinesServiceNotFound();
    }

    anchorLinesService.addAug(aug);
  }

}

exports.default = LocationAnchorLines;