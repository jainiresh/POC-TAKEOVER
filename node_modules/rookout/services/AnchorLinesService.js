"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.AnchorLinesService = void 0;

var _IService = require("./IService");

var _logger = require("../logger");

class AnchorLinesService extends _IService.IService {
  constructor() {
    super();
    this.aug = undefined;
  }

  addAug(aug) {
    if (this.aug === undefined) {
      _logger.logger.debug('Adding Anchor Lines Aug, ' + aug.augId);

      this.aug = aug;
    }
  }

  removeAug(augId) {
    _logger.logger.debug('Removing Anchor Lines Aug, ' + augId);

    this.aug = undefined;
  }

  removeAugTemporarily(augId, duration) {
    _logger.logger.debug("Not removing aug temporarily because it is an anchor lines Aug");
  }

  clearAugs() {
    this.aug = undefined;
  }

  removeAugfromInspector() {}

}

exports.AnchorLinesService = AnchorLinesService;